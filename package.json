{
  "name": "readwise-audio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run -c vitest.node.config.js",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.9",
    "@cloudflare/workers-types": "^4.20241022.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.62.0"
  }
}